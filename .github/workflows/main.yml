name: CI
on:
  push:
jobs:

  build:
   
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello, world!
      - name: git version
        run : git --version
      - name: mvn version
        run : mvn --version
     
